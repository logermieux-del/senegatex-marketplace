version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: yembal-app
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://test:test@db:5432/yembal_test
      REDIS_URL: redis://redis:6379
      MEILISEARCH_URL: http://meilisearch:7700
      NEXTAUTH_URL: http://localhost:3000
      NEXTAUTH_SECRET: dev-secret-change-in-production
      NODE_ENV: development
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      meilisearch:
        condition: service_started
    command: npm run dev
    networks:
      - yembal

  db:
    image: postgres:16-alpine
    container_name: yembal-db
    environment:
      POSTGRES_DB: yembal_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test -d yembal_test"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - yembal

  redis:
    image: redis:7-alpine
    container_name: yembal-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - yembal

  meilisearch:
    image: getmeili/meilisearch:latest
    container_name: yembal-search
    environment:
      MEILI_MASTER_KEY: DebugKey1234567890
      MEILI_ENV: development
    ports:
      - "7700:7700"
    volumes:
      - meilisearch_data:/meili_data
    networks:
      - yembal

volumes:
  postgres_data:
  redis_data:
  meilisearch_data:

networks:
  yembal:
    driver: bridge
